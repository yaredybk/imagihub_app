export default function ClearHistory(props) {
    return (
        <button
            className="bg-red-100 text-red-600 border-red-400 p-1"
            type="button"
            {...props}
        >
            Clear History
        </button>
    );
}
